function login(
  username: string
): string {
  // console.log(username);
  
  const user = {name:"test"
  }

  // console.log(user.name);
  
  user.name
  return username + user.name;
}

login("hi");
